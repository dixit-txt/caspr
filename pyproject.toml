[project]
name = "caspr-core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Amit Kotnala", email = "amit.kotnala@ez.works" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
caspr-core = "caspr_core:main"

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"
